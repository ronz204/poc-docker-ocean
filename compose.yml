name: ocean

include:
  - server/compose.yml
  - docker/postgres/compose.yml

volumes:
  ocean-data:
    driver: local
    name: ocean-data

networks:
  ocean-network:
    driver: bridge
    name: ocean-net
